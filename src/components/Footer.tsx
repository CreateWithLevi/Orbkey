import React from "react";

const Footer = () => {
  return (
    <div className="fixed bottom-0 right-0 bg-white py-2 px-4 z-50">
      {/* Empty footer to cover Spline watermark */}
    </div>
  );
};

export default Footer;
